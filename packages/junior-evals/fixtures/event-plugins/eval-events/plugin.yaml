name: eval-events
display-name: Eval Events
description: Creates eval pull requests with subscribable events

mcp:
  url: https://eval-mcp.example.test/mcp
  allowed-tools:
    - create-watchable-pull-request
