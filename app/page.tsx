export default function HomePage() {
  return (
    <main style={{ padding: 24, fontFamily: "ui-monospace, SFMono-Regular, Menlo, monospace" }}>
      <h1>shim</h1>
      <p>
        Slack webhook endpoint is available at <code>/api/webhooks/slack</code>.
      </p>
    </main>
  );
}
