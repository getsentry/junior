# Eval Coverage Map: `agent-turn-handling`

Existing eval file names are coverage inventory, not the desired long-term taxonomy. This map records which current eval cases cover the model-dependent `agent-turn-handling` scenarios and which taxonomy changes should happen later.

| Scenario                                                        | Current Eval Coverage                                                                                                                                                                                                                                                                                         | Coverage Action              | Notes                                                                                                                                       |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Implicit follow-up after Junior's prior answer                  | `packages/junior-evals/evals/core/passive-behavior.eval.ts`: "when a follow-up is clearly directed at Junior's prior answer, reply without another @mention"; "when a terse clarification comes right after Junior's answer, treat it as directed back to Junior"                                             | keep now, rename/split later | Belongs under future `agent-turn-handling.eval.ts` or equivalent capability taxonomy.                                                       |
| Side-conversation silence                                       | `packages/junior-evals/evals/core/passive-behavior.eval.ts`: human-to-human question, casual pronoun question, same-topic vocabulary, coworker-directed "can you", humans took floor                                                                                                                          | keep now, rename/split later | Strong coverage, but filename is broad rather than capability-scoped.                                                                       |
| Source-backed answer instead of offering to check later         | `packages/junior-evals/evals/core/skill-infra.eval.ts`: "when asked to double-check a source-backed fact, use the source and answer completely"; `packages/junior-evals/evals/core/research-reply-shape.eval.ts`: multi-source summary                                                                        | split later                  | Source-backed behavior should probably move to an `agent-source-use` or `tool/source-use` capability eval.                                  |
| Ask only when blocked / do not ask when action is possible      | `packages/junior-evals/evals/core/routing-and-continuity.eval.ts`: named-channel limitation; `packages/junior-evals/evals/core/scheduler.eval.ts`: clear reminders/schedules do not ask for confirmation; `packages/junior-evals/evals/core/skill-infra.eval.ts`: source lookup does not offer to check later | split later                  | Current coverage is distributed across capability-specific files. Add a direct blocked-decision eval if future prompt changes regress this. |
| Resumed turn answers final requested content with prior context | `packages/junior-evals/evals/core/oauth-workflows.eval.ts`: MCP auth resume with prior context; generic OAuth resume with prior context                                                                                                                                                                       | keep now, split later        | These also belong to `oauth-flows` and `agent-session-resumability`; `agent-turn-handling` owns only final-answer policy.                   |

## Gaps / Follow-Up

- Rename or split current eval files only after the broader backfill taxonomy is accepted.
- Add a focused "missing access requires one focused question" eval if future work changes prompt wording or tool access behavior.
- Do not duplicate deterministic Slack routing assertions in evals; keep those in unit/integration coverage.
