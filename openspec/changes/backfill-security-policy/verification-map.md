# Security Policy Verification Map

| Capability      | Requirement                         | Scenario                                                                         | Primary Layer    | Current Coverage                                                                     | Action     | Gap / Notes                                                                                   |
| --------------- | ----------------------------------- | -------------------------------------------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------ | ---------- | --------------------------------------------------------------------------------------------- |
| Security policy | Secret custody                      | Host-only long-lived secrets                                                     | Unit/manual      | `github-app-broker.test.ts`, `sentry-broker.test.ts`, config tests                   | keep       | Manual review still needed for new secret env bindings.                                       |
| Security policy | Sandbox isolation                   | User-influenced command execution uses sandbox                                   | Integration/unit | `sandbox-executor.test.ts`, `sandbox-tools` backfill map                             | split      | Detailed command behavior owned by `sandbox-tools`.                                           |
| Security policy | Network egress allowlist            | Registered provider domains become forwarded policy rules                        | Unit             | `sandbox-egress-proxy.test.ts` policy cases                                          | keep       | Platform enforcement is not fully simulated locally.                                          |
| Security policy | Egress request authentication       | Missing/invalid OIDC rejected before routing                                     | Unit             | `sandbox-egress-proxy.test.ts` OIDC cases                                            | keep       | Uses injected verifier for most proxy tests; JWKS path has unit coverage.                     |
| Security policy | Forwarded routing validation        | Host/scheme/port/path validation rejects unsafe values                           | Unit             | `sandbox-egress-proxy.test.ts` forwarded routing cases                               | keep       | Exact Vercel header semantics should be rechecked when platform changes.                      |
| Security policy | Requester-bound credential issuance | Signed requester context required and sandbox id must match                      | Unit             | `sandbox-egress-proxy.test.ts` requester token cases                                 | keep       | Good deterministic coverage.                                                                  |
| Security policy | Lazy credential issuance            | Registration does not mint credentials until matching forwarded request          | Integration/unit | `sandbox-egress-proxy.test.ts`, `sandbox-egress-proxy.integration.test.ts`           | keep       | Covered by no command-session-state integration path.                                         |
| Security policy | Lease scoping and refresh           | Lease scoped by provider/requester/sandbox/context; cleared after auth rejection | Unit             | `sandbox-egress-proxy.test.ts` lease cache cases                                     | keep       | Exact TTL values are implementation detail.                                                   |
| Security policy | Header transforms                   | Real secrets injected only in host request headers                               | Unit/integration | `sandbox-egress-proxy.test.ts`, `github-app-broker.test.ts`, `sentry-broker.test.ts` | keep       | Add tests when new broker type is added.                                                      |
| Security policy | Placeholder command env             | Sandbox auth env vars are non-secret placeholders                                | Unit             | `sandbox-egress-proxy.test.ts`, broker tests                                         | keep       | Secret-env allowlist should be reviewed with plugin manifest changes.                         |
| Security policy | OAuth link privacy                  | Auth URL delivered privately and omitted from visible messages/model context     | Integration/eval | `oauth-resume-slack.test.ts`, `mcp-oauth-callback-slack.test.ts`, OAuth evals        | keep/split | Full user-facing behavior owned by `oauth-flows`; evals verify natural-language continuation. |
| Security policy | Authorization session history       | Auth completion projected as host-authored observation                           | Unit/integration | `session-log.test.ts`, OAuth resume tests                                            | keep       | Already incorporated by current auth resume boundary change.                                  |
| Security policy | Provider baselines                  | GitHub App and OAuth bearer brokers issue short-lived host-managed leases        | Unit             | `github-app-broker.test.ts`, `sentry-broker.test.ts`, `api-headers-broker.test.ts`   | keep       | Provider-specific scope/refresh details owned by `plugin-auth`.                               |
| Security policy | Logging redaction                   | Logs omit tokens/private keys/raw Authorization values                           | Unit/manual      | Partial: proxy strips proxy-only headers; policy docs                                | add later  | No broad redaction scanner; require focused tests for new privileged log events.              |
| Security policy | Incident response                   | Rotation, revocation, audit, patch/reverify process exists                       | Manual/policy    | `specs/security-policy.md`                                                           | keep       | Operational runbook depth is outside current code baseline.                                   |
